# 实训笔记

## 一、五子棋（控制台版）

### 重要记忆点

1、对于一个程序来说，要先确保他的数据存储位置，后续的操作是对于数据的打印和修改、当前项目是用一个二位列表使用坐标的形式进行存储

2、几个函数：

```python
print_board(): # 从二维列表中取数据打印出来
put_chess(chess) # 传入下棋的坐标，进行落子
judge_winner(pos, chess): # 传入落子坐标和下棋坐标，判断输赢
```

3、对于方向的判断可以用方向坐标在原始坐标生进行加减来判断输赢。

4、控制台的颜色表达式"`\033[1;...;...m起始头、\033[0m结束头、`"

## 二、学生信息管理 系统

### 重要记忆点

对于一个完整的工程文件，主要接口和附属工具的联合应用，使代码框架具体、类似搭房子

模块化代码书写，包装库，制作对象，菜单界面分开书写，来组成一个程序

`enumerate()函数用于将一个可迭代对象转换为一个索引序列，同时列出数据和数据下标，常用于在循环中获取元素的索引。`

`or 的短路特性，如果用户输入了新的姓名，就使用新的姓名，否则保持原来的姓名不变`

`del students[student] # 通过索引删除学生信息`

`any函数用于判断给定的可迭代对象中是否有满足条件的元素，如果有则返回True，否则返回False`

`next()函数用于从可迭代对象中获取下一个元素，满足条件的学生信息将被返回，如果没有找到则返回None`

### 程序结构

* 学生信息管理系统

  * 程序入口
    * [main()](vscode-file://vscode-app/c:/Users/Altria/AppData/Local/Programs/Microsoft%20VS%20Code/fcf604774b/resources/app/out/vs/code/electron-browser/workbench/workbench.html)
    * 菜单选择
  * 菜单模块
    * 清屏
    * 显示菜单
  * 组件模块
    * 读写数据
    * 输入/查询/删除/修改
    * 排序/统计/显示
  * 数据存储
    * [students.txt](vscode-file://vscode-app/c:/Users/Altria/AppData/Local/Programs/Microsoft%20VS%20Code/fcf604774b/resources/app/out/vs/code/electron-browser/workbench/workbench.html)

## 三、淘宝电商订单分析系统

### 环境准备

Anaconda作为一个集成好的python开发版本、Python 解释器、Jupyter Notebook 以及数据分析常用的第三方库（如 Pandas、NumPy），一步安装即可搞定所有依赖，无需额外配置。多用于数据分析、机械模型开发与实验

Jupyter Notebook

数据分析

### 重要记忆点

## 四、水果消消乐游戏
